apiVersion: 1

datasources:
  - name: Postgres-SciFit
    type: postgres
    access: proxy
    url: postgres:5432
    database: scifit
    user: scifit
    secureJsonData:
      password: scifit_local_pw
    jsonData:
      sslmode: disable
      postgresVersion: 1600
    isDefault: true
